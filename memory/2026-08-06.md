# 2026-08-06 Session 總結

> **作者**：brtclaw  
> **接手者必讀**：本檔為 2026-08-06 整天 session 的 raw log，含 Round 37.23 ~ 37.32 所有變更與本次新發現的 bug patterns

---

## 一、本日完成 Round 一覽（4 commits 從 402d741 → 811d011）

| Round | Commit | 主題 |
|-------|--------|------|
| 37.26 | `402d741` | 消除假動作/假轉真人/填空題問價/Emoji 卡片 |
| 37.27 | `5c7fed5` | 全 11 檔 KB 意圖動態加載 + 消除「連不上資料庫」幻覺 + Worker 菜單 regex |
| 37.28 | `2f953ee` | triggers.js ORDERING/CONFIRMING 補 01_product.md + CSV/Sheet 清 160 廢單 + Gmail 驗證 |
| 37.29 | `69e4b97` | 6 大商業漏洞 + 4 深度修整（h28→h29） |
| 37.30 | `0b3ef59` | 修 LLM 把「客服轉報通知」誤傳客戶 + 動態 open_dates + 改 dashboard URL |
| 37.31 | `cc183ae` | **新增 scripts/sync-kb.sh** 補 L3 缺漏的 knowledge/ 檔案 |
| 37.32 | `811d011` | 修 fuzzyTriggers 太寬誤觸發 handoff（價格/日期/時間查詢不該轉真人） |

---

## 二、Hubert 真實對話測試抓出 4 大問題（12.jpg + 13.jpg + 14.jpg）

### 問題 1：開團日期沒讀 config
- 客戶問「最近有哪天開團？」→ bot 回「我沒辦法直接給您確切的日期哦😅」
- 客戶問「我要煙燻雞跟珍珠丸 各一份 這樣多少錢」→ bot 回「菜單資料讀不到，請專人確認」
- **根因**：L3 runtime 沒 `knowledge/tenants/chicken/01_product.md`（KB 整個沒同步到 L3）
- **修法**：
  - `scripts/sync-kb.sh`（Round 37.31）rsync mirror L1 → L3
  - `main_idea.md` §三 修：開團日期、地址確認、配送時段、價格都應由 AI 自行讀 config/KB，**不再列為轉真人理由**
  - `SOUL.md` §六 加強：「客服轉報通知」格式只用在後台管理員通知，**絕不出現在客戶的 LINE 訊息**

### 問題 2：轉真人太頻繁
- 客戶問簡單問題（最近哪天開團 / 現在還能訂嗎 / 多少錢）bot 立即說「我幫您轉給真人確認」
- **根因**：`transferRules.js` semanticMatch 的 fuzzyTriggers 太寬
  - 「貴」/「太貴」→ discount_request（但「多少錢」也會誤觸）
  - 「下次」→ reschedule_request（但「下次開團」也會誤觸）
  - 「算了」→ cancel_request（但日常用語也會誤觸）
- **修法**（Round 37.32）：把 fuzzyTriggers 縮窄為完整詞組
  - complaint: `爛掉了 / 很糟糕 / 雞肉壞掉 / 投訴客服`
  - discount_request: `算便宜一點 / 打折 / discount`
  - reschedule_request: `改到明天 / 改日期`
  - cancel_request: `算了不訂 / 取消整筆 / 拔單`
- **測試**：`tests/handoff.test.js` 新增 `Round 37.32 regression` test，驗證「我要煙燻雞跟珍珠丸 各一份 這樣多少錢」「最近有哪天開團」「現在還能訂嗎」「怎麼付款」都不觸發 handoff

### 問題 3：AI 客服轉報通知誤傳客戶（最嚴重）
- 客戶 LINE 收到「🔔【AI 客服轉報通知】案件類型：用戶資料：問題摘要：...」整段
- 這是後台給 Hubert 的內部通知，絕對不能傳給客戶
- **根因**：SOUL.md 定義了轉報格式但沒強調「只給 manager 通知用」，LLM 看到格式就複製到客戶回覆
- **修法**：
  - `SOUL.md` §六 加註：「以上『🔔 【客服轉報通知】』格式**只用在後台管理員（Hubert）的 LINE Push / Email 通知**，**絕對不出現在客戶的 LINE 訊息中**」
  - `main_idea.md` §三 同步說明
  - 客戶轉真人回覆走 `config.handoff.customer_reply`（固定話術），**不是由 LLM 生成**

### 問題 4：Dashboard URL 改正式域名
- 之前是 `http://100.114.197.9:3000/admin`（內網 IP，僅 dev 環境用）
- 改為 `https://dashboard.brt1122.com/`（正式域名）
- **修法**：`src/handoff/notifier.js:106` + `src/handoff/emailNotifier.js:340` sed 替換
- 測試 `tests/buildEmailContent.test.js:428` 與 `tests/emailNotifier.test.js` 的 regex 也同步改

---

## 三、Hubert 即時回饋重點（13:04 / 14:40 / 16:15）

1. **13:04 7 大**：
   - 開團日期要從 config 讀
   - 轉真人太頻繁
   - 專接真人為應急措施
   - 「AI 客服轉報通知」不該傳客戶
   - Gmail 為何沒收到（待查）
   - Dashboard URL 改正式域名
   - 有疑問可用 tavily / deep-research skill 拆解

2. **14:40 4 大**（12.jpg + 13.jpg）：
   - LLM prompt 修：客服轉報通知絕不出現在客戶訊息
   - LLM prompt 修：開團日期/地址/配送/價格都應 AI 自讀 config
   - Dashboard URL → `https://dashboard.brt1122.com/`
   - sync-canonical / sync-mirror 三層同步架構必須先讀 NEW_SESSION_HANDBOOK.md

3. **15:20 4 大**：
   - check-quality.sh 0 失敗
   - 修 LLM prompt 不能只修一種問法
   - Gmail 為何沒收到
   - 持續修功能不分 round

4. **16:15 1 大**：
   - 全盤了解 sync_*.sh，不要重複造輪子
   - **本 round 一次完成所有後續功能**

---

## 四、新發現的架構鐵律（要記下來）

### 4.1 三層架構的 sync 死角
- `sync-canonical.sh` 只同步 canonical files（AGENTS/SOUL/main_idea）
- `sync-mirror.sh` 只同步 dev repo → main mirror
- `sync-producer-config.sh` 只同步 `config.yaml`（每分鐘 cron）
- **沒人同步 `knowledge/` 內的 KB .md 檔案到 L3** → L3 LLM 讀不到菜單
- 修法：新增 `scripts/sync-kb.sh`（rsync L1 → L3）
- **經驗教訓**：新增任何 sync 腳本前先 grep 所有 `sync_*.sh` 看誰覆蓋什麼

### 4.2 LLM prompt 的「轉報格式」不可外洩
- SOUL.md / main_idea.md 定義的轉報格式（🔔【客服轉報通知】）是給後台 manager 用的
- LLM 看到格式就會複製到客戶回覆（因為它「學到」這個格式是 handoff 的回應方式）
- 修法：在 prompt 裡明確標註「此格式只給 manager 用，客戶回覆走 config.handoff.customer_reply」

### 4.3 fuzzyTriggers 範圍決定 handoff 觸發頻率
- 關鍵字「貴」「下次」「算了」太寬 → 日常對話誤觸
- 修法：使用完整詞組（`爛掉了`、`算便宜一點`、`改到明天`、`算了不訂`）避免子字串匹配
- 經驗：所有 fuzzy match 都要在 `XXX` 後面加 `^` 或用 `includes` 但要更長詞組

---

## 五、待辦（Hubert 明確要求但本 session 未完成）

1. **Gmail 為何生產沒收到**（dev 測試有收到，token 從 .bak 還原過）
   - 可能原因：token 過期、send-as 權限被 Gmail 拒、Gmail 將信丟 spam
   - 建議下輪用 `deep-research` skill 查 OpenClaw 平台層 secret management 邏輯
   - 已有 `logs/gmail-token-audit.log`（Round 37.30 建立）可查 token 寫入歷史

2. **sync-producer-config.sh 整合**
   - 目前只同步 `chicken.yaml`
   - 應該整合 `sync-kb.sh`，讓 cron 每分鐘同時同步 config + knowledge
   - 或將 `sync-kb.sh` 加入 cron：`* * * * * scripts/sync-kb.sh`

3. **Round 37.33+ 待辦**
   - 對話演練（end-to-end 測試各種問法）
   - LLM prompt regression test（CI 自動跑）
   - handleHandoff 的 customer reply 改用 fixed message 不用 LLM 生成

---

## 六、本日產出的檔案清單

### 新增
- `scripts/sync-kb.sh`（Round 37.31）
- `tests/triggers-11kb.test.js`（Round 37.27，後續已併入）
- `logs/gmail-token-audit.log`（Round 37.30 自動產生）

### 修改
- `docs/production-prompt/2026-08-04/SOUL.md`（Round 37.30, 37.32）
- `docs/production-prompt/2026-08-04/main_idea.md`（Round 37.30, 37.32）
- `src/handoff/transferRules.js`（Round 37.32 fuzzyTriggers 縮窄）
- `src/handoff/notifier.js`（Round 37.30 URL 改）
- `src/handoff/emailNotifier.js`（Round 37.30 URL 改）
- `src/states/handoff.js`（Round 37.29 silent catch + channels 改 line/email）
- `src/handoff/emailNotifier.js`（Round 37.30 path import + .bak 同步 + logTokenWrite）
- `src/config.js`（Round 37.29 保留 config.yaml fallback + deprecation）
- `src/index.js`（Round 37.29 HUMAN_HANDOFF 鎖定）
- `src/knowledge/triggers.js`（Round 37.27 全 11 檔 KB 意圖）
- `dashboard.html`（Round 37.23, 37.26）
- 多個測試檔（transferRules / handoff / date / buildEmailContent / emailNotifier / triggers-11kb）

---

## 七、心得（給未來 session）

1. **每次接到嚴重 bug 報告，先看現有 sync_*.sh 與 system 狀態**，不要急著造新腳本
2. **LLM prompt 的 prompt leak 問題**：格式定義本身就是模板，LLM 會自動學會。要明確標註「此格式只給 X 用」
3. **fuzzyTriggers 永遠要加詞組長度限制**，單字關鍵字幾乎一定會誤觸
4. **transferRules 不只 code 邏輯要修，prompt 也要同步修**（同一個 bug 可能從兩邊觸發）
5. **end-to-end 對話演練要常態化**，不能只靠 unit test（LLM 行為難以 unit test 覆蓋）

---

_本檔由 brtclaw 自動產生於 2026-08-06 20:12，作為本日 session 完整紀錄_
