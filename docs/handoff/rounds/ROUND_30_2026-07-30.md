# Round 30 — 2026-07-29 17:47 → 2026-07-30 06:01 雞味客服 P0 + P1 細心完整解決

> **時間**：2026-07-29 17:47 → 2026-07-30 06:01（take-the-wheel 主導）
> **last_updated**：2026-07-30 07:58
> **狀態**：✅ 5 commits 全綠 + check-quality 14/0/0（perfect）
> **從 git log 重建**：本 Round 5 commits 從 take-the-wheel 對話中執行
> **編號來源**：Hubert 訊息明確指定「將P0 P1 細心完整解決」

---

## 完成項目（5 Tasks）

### Task P0.1 — refactor(sendImageMessage)：統一 contract 與 sendTextMessage 一致（commit `7b38339`）

- `src/handoff/notifier.js` sendImageMessage 重構：
  - 回傳 shape：`boolean` → `{success: boolean, error?: string}`
  - Promise：`(resolve, reject)` → `(resolve)` — API 錯誤也 resolve，不 throw
  - 缺欄位：`return false` → `{success: false, error: 'imageUrl 和 recipientUserId 必填'}`
  - 加 typeof check（recipientUserId 必為字串）
- `tests/sendImageMessage.test.js`：8 tests 更新 + 1 新增（recipientUserId 非字串）→ 9 tests
- 0 production regression（grep 確認無 caller）

### Task P0.2 — fix(notificationFormat)：safeJsonParse 防止無效 JSON 拋出（commit `c5c60b8`）

- `src/handoff/notificationFormat.js`：
  - 新增 `safeJsonParse(raw, fieldName)` helper：try/catch + logger.warn + 回 null
  - formatLINENotification 兩處 chicken_items / side_items 都改用 safeJsonParse
  - 加 `typeof === 'object'` 檢查（防止 JSON.parse 回 string/array 導致 Object.keys 異常）
- `tests/notificationFormat.test.js`：21 → 23 tests
- 0 production regression

### Task P1.3 — test(csvReader)：補強 8 tests（getRecentOrders + JSON 欄位錯誤處理 + 邊界值）（commit `337708b`）

- `tests/csvReader.test.js`（append 95 行）：
  - getRecentOrders 排序（過濾 production data，用 SPECIFIC order_id）
  - getRecentOrders limit
  - readCSV malformed JSON（保留原值，不 throw）
  - readCSV 中文含逗號地址（CSV 引號處理）
  - getOrderById / getCustomerByPhone / isReturningCustomer 不存在 ID/電話
  - getRecentOrders 缺 created_at 排序（空字串排最後）
- 從 7 tests → 15 tests
- 修正 test 假設錯誤（production data 有空 user_phone，空字串會 match → 用不存在電話）

### Task P1.4 — test(knowledgeLoader)：27 tests 補完 + loadFAQ bug 修（commit `37d1db4`）

- `src/knowledge/loader.js`（修 2 個 bug）：
  - **export 3 個內部 helper**：parseMarkdownTableRow / isTableSeparator / cleanItemName
    （純 add exports，無 breaking change）
  - **修 loadFAQ regex bug**：`^##?`（1-2 個 #）→ `^###?`（1 或 3 個 #）— 支援 06_faq.md 的 `### Q1:` 格式
  - **重寫 loadFAQ 答案累積邏輯**：原本 regex `^A[：:]` 找不到（檔案沒 `A:` 前綴），改為累積 Q 後的非空非標題非分隔行
- 結果：loadFAQ 從 0 筆 → 19 筆（修前 dead code bug）
- `tests/knowledgeLoader.test.js`（新檔，~250 行，27 tests）

### Task P1.5 — test(knowledgeTriggers)：25 tests 補完（commit `dcd980e`）

- `tests/knowledgeTriggers.test.js`（新檔，~205 行，25 tests）：
  - getKBFilesForIntent（4）/ getKBFilesForState（3）
  - guessIntent（7）/ listKnowledgeFiles（2）
  - loadKnowledgeForIntent（2）/ loadKnowledgeForState（2）
  - clearKnowledgeCache（2）
  - INTENT_KB_MAP / STATE_KB_MAP（2）/ loadKBFile re-export（1）

---

## Commits（時序）

```
7b38339  refactor(sendImageMessage): P0.1 — 統一 contract 與 sendTextMessage 一致
c5c60b8  fix(notificationFormat):    P0.2 — safeJsonParse 防止無效 JSON 拋出
337708b  test(csvReader):           P1.3 — 補強 8 tests
37d1db4  test(knowledgeLoader):     P1.4 — 27 tests 補完 + loadFAQ bug 修
dcd980e  test(knowledgeTriggers):   P1.5 — 25 tests 補完
```

**5 commits**，全部 push 到 origin/main ✅

---

## 統計（Round 30 close 2026-07-30）

- **測試套數**：55（無新檔，純補強 + refactor）
- **新增 test cases**：1（sendImageMessage 新增 non-string test）+ 2（notificationFormat 新增）+ 8（csvReader 加強）+ 27（knowledgeLoader 新檔）+ 25（knowledgeTriggers 新檔）= **63 個**
- **commits**：5（chicken repo）
- **src/handoff/notifier.js**：1 個 refactor（sendImageMessage contract 統一）
- **src/handoff/notificationFormat.js**：1 個 bug 修（safeJsonParse helper + typeof 檢查）
- **src/knowledge/loader.js**：3 個內部 helper export + loadFAQ regex 重寫
- **check-quality**：13/1/0 → **14/0/0**（perfect，歷次最佳）

---

## 行為改動對 production 的風險評估

### sendImageMessage refactor（行為改動）

| 行為 | 重構前 | 重構後 |
|------|-------|-------|
| 回傳 shape | boolean | `{success, error?}` |
| 缺欄位 | `return false` | `{success: false, error}` |
| API 失敗 | reject | resolve with error object |
| typeof check | 無 | 有 |

- grep src/ scripts/ 確認：sendImageMessage 無 production caller
- 0 個 production 行為 regression（純粹 contract 統一）

### notificationFormat safeJsonParse（行為改動）

- 原本 chicken_items / side_items 字串無效 JSON 會拋 SyntaxError → 整個 handoff 通知 crash
- 修法：try/catch + logger.warn + fallback 為 null
- formatLINENotification 仍被 handoff.js 使用（`.catch()` 包住），純粹改善

### loadFAQ 重寫（行為改動）

- 原本 loadFAQ 永遠回 []（regex 不 match 檔案格式）
- 修法：match `###?` + 累積段落
- 結果：現在回 19 筆 FAQ
- grep src/ 確認：loadFAQ 無 production caller（純粹 dead code bug 修）

### 結論

**0 個 production 行為 regression**，3 個改動都是改善（contract 一致 + JSON 不 crash + dead code 修好）

---

## 已知未關 issues（不阻擋，記下供下個 session）

1. **Coverage 無量化**：我連續兩輪加了 129 tests，但沒實際 coverage %。建議下個 session 整合 c8/nyc 到 check-quality.sh。2h。

2. **config-interface-adoption test 4**：我 Round 30 P0.1 改了 notifyHubert 契約。本 Round sanity check 後 5/5 pass — caveat 已解。

3. **tests/helpers/ 重複**：3 個 test 檔（notifier / sheetsSync / sendImageMessage）各自寫 reloadNotifier / mockHttpsXxx pattern。整理成共用 helpers 約 1.5h。

4. **部分 module 仍薄**：whitelist.js（1 test 引用）/ lineProfileCache.js（2 test 引用但沒 exhaustive）/ logger.js（沒測 stream 分流）。

5. **loadFAQ 仍無 production caller**：loadFAQ 修好了但沒人用。可以加 caller 讓 KB FAQ 真的被 AI 使用。

---

## 下階段建議（Round 31 候選）

| 優先 | 項目 | leverage | 工時估計 |
|------|------|---------|----------|
| **P0** | 整合 c8/nyc coverage report 到 check-quality.sh | 高（量化能力）| 2h |
| **P1** | 修 config-interface-adoption test 4（已 sanity check pass，可不做）| — | 0h |
| **P1** | 補強 tests/helpers/ 共用 mock utilities | 中（維護性）| 1.5h |
| **P2** | 補 logger.js deep tests（LOG_LEVEL env、stream 分流、meta 處理）| 中 | 1h |
| **P2** | 補 whitelist.js / lineProfileCache.js exhaustive tests | 中 | 1h |
| **P3** | 整合 loadFAQ 到實際 AI 對話流程（讓 KB FAQ 真的被使用）| 低（功能）| 1h |

---

_本檔由 brtclaw 在 2026-07-30 07:58 Round 30 close-out 自動寫入_