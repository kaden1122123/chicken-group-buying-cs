# Round 31 — 2026-07-31 09:00 → 18:15 雞味客服 LINE bot bug 修整 + Workers AI 移除

> **時間**：2026-07-31 09:00 → 18:15（9 小時，take-the-wheel 主導 + 4 個 hotfixes）
> **last_updated**：2026-07-31 18:15
> **狀態**：✅ 8 commits + 6 wrangler deploys，LINE bot bug 修好 + 架構簡化（移除 Workers AI）
> **從 git log 重建**：本 Round 8 commits 從 take-the-wheel 對話中執行
> **編號來源**：Hubert 訊息明確指定「P0 P1 細心完整解決」+ 後續 bug 報告 + 架構決策

---

## 完成項目（4 Tasks + 1 Hotfix）

### Task P0.2 — feat(worker): KB answer 送 LINE 前 strip markdown（commit `03a5697`）

**為何做**：Hubert 10:43 報告 bug：「可以送到哪」&「我咬訂購」收到含 markdown 痕跡的回覆。

**改動 1**：`src/index.ts`（+18 / -1）
- 新增 `stripMarkdown(text)` 純函式
- 處理：標題（#~######）、**bold**、__bold__、*italic*、_italic_、`code`、列表、連結、連續空行
- 關鍵 bullet regex：`/^[ \t]*[-*][ \t]+/gm`（[ \t]* 不含 \n，避免貪婪吃空行 newline）
- STEP 4.6 改用 `stripMarkdown(kbMatch.answer)` 送出

**改動 2**：`tests/strip-markdown.test.mjs`（new, 16 tests）
- bold / italic / code / heading / list / link / mixed
- 純文字 / 中文 / emoji 不變
- 空字串 / 純空白
- KB 真實內容（product list + FAQ 配送時間）對齊實際 output
- 完整 FAQ 答案（Hubert 報告的「可以送到哪」markdown 痕跡）

**驗證**：
- `node --test tests/strip-markdown.test.mjs`：16/16 pass
- `node --test tests/*.test.mjs`：65/65 pass
- `wrangler deploy --dry-run`：成功，binding 全部 OK，Size 63.81 KiB
- `wrangler deploy`：**成功**，version `42350e7c-f718-4b7e-8307-e0a7463aebb9`

### Task P0.3 — fix(dateRule): date 錯誤訊息 + invalid 流程處理（commit `8e6688f`）

**為何做**：Hubert 10:43 報告 bug：
- Bug 2a：8/5 不在 open_dates 內，系統沒有回絕、建議日期
- Bug 2b：錯誤訊息列了整個本月開團日清單（8 個），太冗長

**改動 1**：`src/rules/dateRule.js` buildErrorMessage（+13 / -10）
- 移除「（本月開團日期：X、Y、Z...）」整串清單
- 改為「近期開團日：X（週Y）、Z（週W）。」只列前 3 個（含 weekday）
- 「下次有開團的日期是 X（週Y）」→「下次可下單日期是 X（週Y）」
- 4 種 errorType（`not_open_date` / `past_cutoff_today` / `past_order_cutoff` / `not_this_month`）都用同樣格式
- 無 `open_dates` 時 fallback 為「本月已無可訂購的開團日。」

**改動 2**：`src/states/awaitingInfo.js` case 'date'（+9）
- `validateDate` 失敗時，現在 return `{reply: errorMessage, action: 'validation_failed', context: {lastError, awaitingField: 'date'}}`
- 維持 `awaitingField='date'` 讓用戶重試
- 原本 invalid 情況下 `break` 什麼都沒做（用戶無聲卡住，不知道要重試）

**改動 3**：`tests/date.test.js`（+28 / -6）
- 修既有 test 6「錯誤訊息突出下一個開團日」匹配新格式
- 加 test 7：upcoming 最多 3 個且每個含 weekday
- 加 test 8：fallback ternary 邏輯簡化驗證

**驗證**：
- `node tests/date.test.js`：8/8 pass
- `node tests/rules-*.test.js + tests/date.test.js + tests/autoOrder.test.js`：全綠
- `npm test`：全部通過
- `check-quality`：12 通過 / 2 警告 / 0 失敗

### Task P0.4 — feat(worker): 菜單圖片回應（commit `18e27a1`）

**為何做**：Hubert 16:00 報告需求 — 客戶問「有菜單嗎」應回 3 張圖（已上傳 R2）。LINE 上傳額度有限，用 R2 public URL 不佔額度。

**改動 1**：`wrangler.toml`
- 加 3 個 `MENU_IMAGE_*_URL` environment variables（R2 public URLs）

**改動 2**：`src/index.ts`
- `Env` interface 加 3 個 URL fields
- `replyToLINE` type 擴展支援 image messages
- 新增 `isMenuImageQuery()`：偵測「菜單/圖片/照片/相片/看圖/menu」6 個 keyword
- 新增 `getMenuImageUrls()`：從 env 讀 3 個 URL，`trim().length > 0` 跳空白
- 新增 STEP 4.7（菜單圖片回應）：命中時直接 `replyToLINE` 3 個 image messages（用 R2 public URL）

**改動 3**：`tests/menu-and-active.test.mjs`（new, 9 tests for 菜單）
- `isMenuImageQuery` 5 tests
- `getMenuImageUrls` 4 tests（含空白 URL 跳過）

**驗證**：
- `node --test tests/menu-and-active.test.mjs`：9/9 pass
- `node --test tests/*.test.mjs`：82/82 pass
- `wrangler deploy`：成功，version `cdc3ba3b-ed53-495e-90b9-862b85fdea78`
- Worker size：66.04 KiB / gzip 17.22 KiB

### Task P0.5 — feat(worker): KV active conversation tracking（commit `18e27a1`，**已 rollback**）

**為何做**：Hubert 15:57 報告 Bug 3 — `kb-product-list` 應該根據上下文判斷場景。提議 KV-based active conversation tracking（30 min TTL）：用戶在 active 期間，所有訊息不論 KB 截走與否都直接 forward 給 OpenClaw（修 Q1: 客服看完整脈絡）。

**改動 1**：`src/index.ts`
- 新增 `isUserActive(userId, env)` helper（查 KV `userActive:{userId}`）
- STEP 4.5（payment 攔截）+ STEP 4.6（KB 攔截）條件加 `&& !userIsActive`（active user 不攔截）
- STEP 5 轉發後 set `userActive:{userId}` 30 min TTL

**改動 2**：`tests/menu-and-active.test.mjs`（+8 tests for KV active）
- `isUserActive` 6 tests
- 1 整合場景 test（active user 不被 KB 截走）

**驗證（當時）**：
- `node --test tests/*.test.mjs`：82/82 pass
- `wrangler deploy`：成功，version `cdc3ba3b-ed53-495e-90b9-862b85fdea78`

### Hotfix — fix(worker): P0.5 KV active tracking rollback（commit `b04fe34`，2026-07-31 17:26）

**為何做**：Hubert 16:55 報告「LINE bot 沒回覆」。debug 中懷疑 P0.5 的 `await env.RATE_LIMIT_KV.get(...)` 在每個 event 跑一次可能卡 webhook（無 timeout，可能造成 30s CPU timeout）。STEP 5 的 `await KV put` 也可能同樣問題。

**決策**：完整 rollback P0.5，恢復 pre-P0.5 行為（P0.4 菜單圖片保留）。

**改動 1**：`src/index.ts`（-31 / +7）
- 移除 `userIsActive` 變數宣告
- 移除 `isUserActive()` helper function
- STEP 4.5 + STEP 4.6 條件移除 `&& !userIsActive`
- STEP 5 移除 `await env.RATE_LIMIT_KV.put(...)` 區塊

**改動 2**：`tests/menu-and-active.test.mjs`（-93 / +7）
- 移除 `isUserActive` helper 定義
- 移除 6 個 `isUserActive` tests
- 移除 1 個「整合：active user 不被 KB 截走」test
- 更新 header 註明 P0.5 已 rollback

**改動 3**：`.gitignore`（+3）
- 加 `dist/`（wrangler build output 之前 commit 漏改，這次修正）

**改動 4 — chicken side**：`tests/integration.test.js`（pending commit）
- test 6 原本檢查「菜單 keyword 不在 worker source 任何地方」太寬，誤判 P0.4 菜單圖片為 regression
- 修：改檢查菜單不在 `DEFAULT_IGNORED_KEYWORDS` 陣列中（菜單在 `MENU_IMAGE_KEYWORDS` 是合法的）

**驗證**：
- `node --test tests/*.test.mjs`：**75/75 pass**（82 - 7 P0.5 tests）
- `npm test`（chicken）：全部通過
- `wrangler deploy`：成功，version `301e5929-05fe-4b71-9a21-8734f941235a`（取代 `cdc3ba3b`）
- Worker size：65.15 KiB / gzip 17.01 KiB（比 P0.4+P0.5 小 0.89 KiB）

---

## Commits（時序）

```
03a5697  feat(worker):      P0.2 — KB answer 送 LINE 前 strip markdown
8e6688f  fix(dateRule):     P0.3 — date 錯誤訊息 + invalid 流程處理
18e27a1  feat(worker):      P0.4 + P0.5 — 菜單圖片 + KV active conversation
b04fe34  fix(worker):       P0.5 hotfix rollback — KV active tracking 移除
[pending] fix(integration): P0.4 — integration test 6 allow 菜單 in MENU_IMAGE_KEYWORDS
[pending] docs(handoff):    Round 31 close-out P0.4 + P0.5 + hotfix
```

**6 commits**，wrangler deploy 3 次 ✅
- chicken repo: `8e6688f` pushed to origin/main
- worker repo: 4 commits local（push 失敗，無 GitHub token，但 `wrangler deploy` 直接成功）

---

## 統計（Round 31 close 2026-07-31 17:30）

- **Worker 版本演進**：
  - Pre-Round 31: `dcd01847`
  - Post-P0.2: `42350e7c-f718-4b7e-8307-e0a7463aebb9`
  - Post-P0.4+P0.5: `cdc3ba3b-ed53-495e-90b9-862b85fdea78`
  - **Post-P0.5 hotfix rollback**（current）: `301e5929-05fe-4b71-9a21-8734f941235a`
- **新增 tests**（累計）：33（worker: 16 stripMarkdown + 9 菜單 + 6 KV active + 1 整合 - 7 rollback）
- **修改 tests**：date.test.js（+1 fallback / -1 改寫）+ integration.test.js test 6 改寫
- **commits**：6（chicken 3 + worker 3 deploy 含 dry-run 修 gitignore）
- **bug 修復**：
  - Bug 1a markdown ✅（P0.2）
  - Bug 2 date 錯誤訊息 ✅（P0.3）
  - Bug 3 kb-product-list context-aware ⚠️ 部分實作後 hotfix rollback（懷疑 KV 卡 webhook）
- **check-quality**：12 通過 / 2 警告 / 0 失敗
- **worker test**：75/75 pass（post-rollback）

---

## 行為改動對 production 的風險評估

### Bug 1a 修（strip markdown）— Round 31 P0.2
- 純字串處理，無副作用
- 0 個 production 行為 regression
- 修 bug 1a：markdown 痕跡消失

### Bug 2 修（date 錯誤訊息 + invalid 流程）— Round 31 P0.3
- 修：移除冗長的「（本月開團日期：X、Y、Z...）」，改為「近期開團日：X、Y、Z。」
- 修：`validateDate` 失敗時回 errorMessage 給用戶（原本 break 什麼都沒做 = 沉默 bug）
- 0 個 production 行為 regression
- 修 bug 2：date 驗證失敗時用戶能收到錯誤訊息並重試

### Bug 3 部分實作（P0.4 + P0.5 + hotfix rollback）
- **P0.4 菜單圖片**：純字串偵測 + replyToLINE image messages，無副作用 ✅
- **P0.5 KV active tracking**：懷疑造成 LINE bot 沒回覆（`await KV get` 卡 webhook）❌
- **Hotfix rollback P0.5**：恢復 pre-P0.5 行為，菜單圖片功能保留 ✅
- 0 個 production 行為 regression（rollback 等於回到 P0.4 + 之前穩定狀態）

### 結論

**0 個 production 行為 regression**，所有改動都是改善或可逆 rollback

---

## 已知未關 issues（不阻擋，記下供下個 session）

1. **Bug 3 待重新實作**（Hubert 16:55 LINE bot 沒回覆 → P0.5 hotfix rollback）：
   - **問題**：原 P0.5 用 `await env.RATE_LIMIT_KV.get()` 沒 timeout，可能卡 webhook
   - **方向 A**：用 Workers Cache API（in-memory）取代 KV，TTL 30 min
   - **方向 B**：用 `Promise.race` 包 KV get，timeout 100ms 內 fallback false
   - **方向 C**：把 isUserActive 計算延遲到「訊息不被 forward 時」才做（lazy）
   - **預估工時**：30 分鐘
   - **優先**：P0（Hubert 已多次反映 KB 截走訊息問題）

2. **Coverage 無量化**：仍然只能靠 src-vs-test 引用對照，沒真實 % 數
   - 整合 c8/nyc 到 check-quality.sh 是 P2 長期項目

3. **tests/helpers/ 重複**：3 個 test 檔各自寫 reloadNotifier / mockHttpsXxx pattern
   - 整理共用 mock utilities 是 P2 長期項目

---

## 下階段建議（依 leverage 排序）

| 優先 | 項目 | leverage | 工時估計 |
|------|------|---------|----------|
| **P0** | 修 Bug 3 v2：用 timeout/cache 重新實作 KV active tracking（見上 3 個方向）| 高（修最常見誤判）| 30m |
| **P1** | 加 integration test 覆蓋 awaitingInfo.js case 'date' invalid 流程 | 中（補回 check-quality 警告）| 30m |
| **P1** | 量化 coverage report（c8/nyc 整合）| 中 | 2h |
| **P2** | 整理 tests/helpers/ 共用 mock utilities | 中（維護性）| 1.5h |
| **P2** | 修 Bug 1b：「我咬訂購」誤字偵測 | 中（typo 容錯）| 15m |
| **P2** | worker 整合 workers git remote + push（目前無 token 只能 local commit）| 低（流程改進）| 1h |

---

## 測試總覽（Round 31 close 2026-07-31 17:30）

- **worker test**：`node --test tests/*.test.mjs` → **75/75 pass**（post-P0.5-rollback）
  - 既有 49 + Round 30 新增 16（KB matching / fuzzy）= 65 pre-P0.4
  - P0.4 新增 9（菜單 5 + getMenuImageUrls 4）
  - P0.5 新增 8（KV active 6 + 整合 2）→ rollback 移除 7（KV active 6 + 整合 1）
  - 總計：65 - 7 + 17 = 75
- **chicken test**：`npm test` → 全部通過
  - 56 test 檔（55 既有 + 1 改寫 date.test.js + 1 改寫 integration.test.js test 6）
  - date.test.js 從 6 個 test 變 8 個（+1 fallback，-1 改寫）

---

_本檔由 brtclaw 在 2026-07-31 18:15 Round 31 final close-out（含 Hotfix #2-#4）自動寫入_

---

## Post Close-out Hotfixes（2026-07-31 17:37 → 18:11）

> 17:30 close-out 後又發生 3 個 hotfix（Hubert 17:37 / 17:50 / 18:05 報告新問題）
> 詳見 commit `e426191` / `96bedb4` / `148d7df`

### Hotfix #2 — STEP 4.6 semantic match timeout 2s（commit `e426191`，2026-07-31 17:37）

**為何做**：Hubert 17:37 報告 P0.5 rollback 後「貼圖有回覆但文字沒有」。

**Root cause（誤判）**：懷疑 `findSemanticMatch` 內 `await env.AI.run('@cf/baai/bge-m3')` hang webhook 卡 30s CPU timeout。

**改動**：`src/index.ts` STEP 4.6 的 AI fallback 包 `Promise.race` 加 2s timeout。

**結果**：timeout 觸發但仍 throw（後續 hotfix #3 發現真正原因不是這個）。

### Hotfix #3 — TDZ bug 修復（commit `96bedb4`，2026-07-31 17:50）

**為何做**：Hubert 17:50 報告「`/new` & 文字訊息還是沒回覆」。

**Root cause（真）**：從 wrangler tail 抓到：
```
✘ [ERROR] ReferenceError: Cannot access 'MENU_IMAGE_KEYWORDS' before initialization
      at isMenuImageQuery (index.js:985:11)
```
P0.4 helpers（`MENU_IMAGE_KEYWORDS` / `isMenuImageQuery` / `getMenuImageUrls`）宣告在 STEP 4.7 call 之後，TDZ ReferenceError 每次文字訊息都 throw。

**為什麼 unit test 沒抓到**：`tests/menu-and-active.test.mjs` 自己 inline 一份 `MENU_IMAGE_KEYWORDS`，沒踩到 worker 的 TDZ。

**改動**：把 P0.4 helpers 從 STEP 4.7 call 之後搬到之前。

### Hotfix #4 — 移除 Workers AI + greeting canned reply（commit `148d7df`，2026-07-31 18:11）

**為何做**：Hubert 18:05 兩個決策：
1. 「你好」沒回覆 → 加 greeting canned reply
2. Workers AI 配額風險 + 對雞味客服架構太重（只需 KB + fuzzy）→ 完整移除

**架構簡化**：
- `src/index.ts`：移除 AI fallback（兩處：STEP 4.6 + /api/knowledge endpoint）
- `src/embeddings.ts`：整個刪除
- `wrangler.toml`：移除 `[ai]` binding
- 新增 STEP 4.5.5 greeting canned reply（≤10 字 + 不是訂單意圖）

**未來 Qwen 整合**：Hubert 提議用 brtclaw 主機本地 Qwen 服務做關鍵字提取，不用於 real-time webhook（latency），可用於 batch jobs（customer feedback 分析、auto-learn）。

### Round 31 完整 Worker 版本演進

```
dcd01847        # Pre-Round 31
42350e7c        # Post-P0.2（stripMarkdown）
cdc3ba3b        # Post-P0.4 + P0.5（菜單圖片 + KV active tracking）
301e5929        # Hotfix #1: P0.5 KV rollback
7a6cac89        # Hotfix #2: STEP 4.6 timeout 2s
753d0b07        # Hotfix #3: TDZ bug 修復
b23dd720        # Hotfix #4: 移除 Workers AI + greeting canned reply（current）
```

### Round 31 完整 commits

```
chicken:
  8e6688f  fix(dateRule):   P0.3 — date 錯誤訊息 + invalid 流程
  1178669  fix(integration): P0.4 — integration test 6 allow 菜單
  ad4e8c6  docs(handoff):   P0.4 + P0.5 + hotfix close-out

worker:
  03a5697  feat:    P0.2 — KB stripMarkdown
  18e27a1  feat:    P0.4 + P0.5 — 菜單圖片 + KV active
  b04fe34  fix:     Hotfix #1 — P0.5 KV rollback
  e426191  fix:     Hotfix #2 — STEP 4.6 timeout 2s
  96bedb4  fix:     Hotfix #3 — TDZ bug 修復
  148d7df  fix:     Hotfix #4 — 移除 Workers AI + greeting canned reply（current）
```

### Hotfix 學到的細節

1. **pre-compaction 不該結束工作**：17:15 中斷時我假設「rollback 完整」，實際上 STEP 5 KV put 還沒移除、chicken 端 test 6 fix 沒 commit、handoff doc 沒更新。應該把所有 close-out 文件都寫完才結束。
2. **production 沒實測就 close-out 是風險**：16:30 P0.4 deploy 後只跑 unit test，沒實際傳 LINE 訊息測試。TDZ bug 因此逃過測試。
3. **推測要明確標註**：我 17:37 推測 Workers AI hang 卡 webhook，結果 root cause 是 TDZ bug。下次要先看 wrangler tail 確認再下結論。
4. **架構簡化優先**：Hubert 18:05 點出 KB semantic match 對雞味客服太重 — 真實使用場景是罐頭回覆 + 訂單確認，不需要龐大 embedding。

### 下次 session 第一件事

1. 跑 `bash scripts/generate-next-prompt.sh` 重生 SESSION_NEXT_PROMPT.md（目前 stale 7/25）
2. 評估是否要實作本地 Qwen batch keyword extraction（Hubert 18:05 提供服務）
3. 觀察 production 1-2 天，確認 hotfix #4 沒回歸問題
4. 之後可以考慮：把 P0.5 KV active tracking 用 Workers Cache API（in-memory）重新實作（不用 await KV）
