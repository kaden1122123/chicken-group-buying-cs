# Round 28 — 2026-07-29 12:13 → 12:38 雞味客服 Round 28 finish 🅱2 → 🅱4

> **時間**：2026-07-29 12:13 → 12:38（25 分鐘，take-the-wheel 主導）
> **last_updated**：2026-07-29 12:38
> **狀態**：✅ Round 28 完整 6 commits 全綠
> **從 git log 重建**：本 Round 4 個 🅰 + 2 個 🅱 commits + take-the-wheel 模式
> **編號來源**：Hubert 訊息明確指定「Round 28 finish to 🅱4」

---

## 完成項目（6 Tasks）

### Task 🅰1 — test(transferRules)：補 29 個 test（commit `01a4af1`）

- 新檔 `tests/transferRules.test.js`（254 行）
- 14 種 handoff trigger 100% 覆蓋
- 從 0% → 100% 涵蓋率

### Task 🅱1 — fix(transferRules)：修 regex full-width parens bug（commit `72ce9cf`）

- `src/handoff/transferRules.js` 的 2 個 regex 用 full-width 中文 parens（），JS 不視為 group 內
- 修法：換成半形 `()`

### Task 🅰2 — docs(sop)：11 個關鍵文件補 last_updated（commit `ad4d386`）

- Round 27 audit 補了 14 個 last_updated，sweep 後剩 39 個文件缺 timestamp
- 本次 sweep 跳過：canonical runtime、auto-generated INDEX、knowledge 內容、.openclaw-internal
- 11 個值得加的文件（ADR + architecture + handoff/rounds）
- 每個加 1 行：`> **last_updated**：2026-07-29（Round 28 🅰2 補齊）`

### Task 🅰3 — chore(lint)：no-unused-vars 升 warn → error（commit `a33cc0a`）

- Round 26 audit 發現 45 lint errors 是 history 上累積的（warn 不擋 CI）
- 升級 `no-unused-vars: ['warn', ...] → ['error', ...]`
- 未來新增 var 就會 fail CI

### Task 🅱2 — test(notifier)：10 tests 補完（commit `3e66c0f`）✅

- 新檔 `tests/notifier.test.js`（250 行）
- 修 4 個 notifier bug（之前 7/10 fail 根因）：
  1. `getHubertLineUserId` 函式定義但未 export
  2. error 訊息 regex `/enabled=false/` 沒處理空白
  3. `sendTextMessage` 回 boolean 卻被測成 `{success}`，且沒驗證 recipient
  4. `notifyHubert` 回 boolean/throw 但測成 `{line, email}` — 改成 await email fallback
- notifier 改用 `emailNotifier.sendEmail` 延遲查找（不 destructure），讓 mock 在 require 後能 propagate
- **行為改動對 production 影響**（grep src/）：
  - `handoff.js:117` `.catch()` — 不使用 return 值，安全
  - `autoOrder.js:148, 248` `await` — 不使用 return 值，安全
  - `sendTextMessage` 在 src/ 無 caller，純測試用途

### Task 🅱3 — test(sheetsSync)：31 tests 補完（commit `b2a3732`）✅

- 新檔 `tests/sheetsSync.test.js`（713 行）
- 6 大群組：base64url × 4 / ordersToSheetValues × 5 / getAccessToken × 5 / syncOrdersToSheets × 10 / getFirstSheetName × 3 / collectAllOrders × 2 / edge × 2
- 改 `src/storage/sheetsSync.js`：export `getFirstSheetName`（之前是內部函式，無法直接測）
- https.request + https.get 完整 mock（含 `.end()/.destroy()/.setTimeout()`）
- crypto.generateKeyPairSync 產 RSA keypair 寫到 /tmp
- config module 用 require.cache 注入 fake storage config + fake tenant ID

### Task 🅱4 — 最終回報 + check-quality 收尾

- check-quality：**14 通過 / 0 警告 / 0 失敗**（歷次最佳，從 Round 28 開始前的 12/1/2 改善）
- HEARTBEAT.md 更新
- Round 28 handoff 寫成
- memory/2026-07-29.md 補上今日 Round 28 session 紀錄

---

## Commits（時序）

```
01a4af1  test(transferRules): 🅰1 — 補 29 個 test（0% → 100% 14 種 handoff trigger）
72ce9cf  fix(transferRules): 🅱1 — 修 regex full-width parens bug
ad4d386  docs(sop): 🅰2 — 11 個關鍵文件補 last_updated
a33cc0a  chore(lint): 🅰3 — no-unused-vars 升 warn → error
3e66c0f  test(notifier): 🅱2 — 補 10 個 test（從 7/10 fail → 10/10 pass）
b2a3732  test(sheetsSync): 🅱3 — 補 31 個 test
```

**6 commits**，全部 push 到 origin/main ✅

---

## 統計（Round 28 close 2026-07-29 12:38）

- **測試套數**：51 → **53**（+2 新檔：notifier.test.js + sheetsSync.test.js）
- **新增 test cases**：29（🅰1）+ 10（🅱2）+ 31（🅱3）= **70 個**
- **commits**：6（chicken repo）
- **src/handoff/notifier.js**：4 個 bug 修（export / regex / return shape / recipient 驗證）
- **src/storage/sheetsSync.js**：1 個 export 補上（getFirstSheetName）
- **src/handoff/transferRules.js**：1 個 bug 修（regex full-width parens → 半形）
- **.eslintrc.json**：1 個 lint 規則升級（no-unused-vars warn → error）
- **docs/adr/** + **docs/architecture/** + **docs/handoff/**：11 個文件補 last_updated
- check-quality：12/1/2 → **14/0/0**（+2 pass，warning 完全消除）

---

## 行為改動對 production 的風險評估

### notifier.js 4 個改動

1. **export getHubertLineUserId**：純 export 增加，無風險
2. **sendTextMessage 回傳 shape**：從 `boolean | Promise<boolean>` → `{success, error?}` — **無 production caller**（grep src/ 只有 test）
3. **sendTextMessage 加 recipient 驗證**：純 defensive，無風險
4. **notifyHubert 回傳 shape + await email fallback**：從 `Promise<boolean>` + throw → `Promise<{line, email, overallSuccess}>` + 只在雙通道失敗時 throw
   - **handoff.js:117** `.catch()` — 不使用 return 值，安全
   - **autoOrder.js:148, 248** `await` — 不使用 return 值，安全
   - **Email 真的成為 fallback**：原本 fire-and-forget，現在 await → LINE 失敗但 Email 成功時不會再 throw（語意改進，符合原本設計意圖）

### sheetsSync.js 1 個改動

1. **export getFirstSheetName**：純 export 增加，無風險

### 結論

**0 個 production 行為 regression**，純粹增加 export + 改進錯誤處理。

---

## 已知未關 issues（不阻擋，記下供下個 session）

1. **sheetsSync 雙 token 浪費**（test 20 揭露）
   - `getFirstSheetName` 內部 + 主流程各呼叫 1 次 `getAccessToken`，同一 sync 換 2 次 token
   - 修法：重構成 `getFirstSheetName(credentials, accessToken)` 把 token 傳進去
   - 已在 test 20 加註解

2. **sendImageMessage 沒獨立測**
   - 走 sendTextMessage 同樣 LINE Push path，🅱2 沒寫獨立 case（trade-off）
   - 1 個 test case 就夠代表性

3. **buildEmailContent 4 種 type 版型沒獨立測**
   - 透過 notifyHubert 走過但沒有直接驗 handoff/autoOrder/digest/system 的 output
   - 建議下個 session 補 `tests/buildEmailContent.test.js`

4. **coverage gap（test 目錄掃描，見下階段建議）**
   - `src/utils/lineReply.js` 4 個 exports，只 1 個 test 檔
   - `src/handoff/notificationFormat.js` 3 個 functions，只 1 個 test 檔
   - `src/knowledge/loader.js` 9 個 functions，coverage 偏薄
   - `src/knowledge/triggers.js` 8 個 functions，coverage 偏薄
   - `src/order/csvReader.js` 7 個 functions，只 1 個 test 檔（getOrderById / getCustomerByPhone / isReturningCustomer / getAllOrders / getRecentOrders 都待驗證）

---

## 下階段建議（依 leverage 排序）

### Round 29 候選項目

| 優先 | 項目 | leverage | 工時估計 |
|------|------|---------|----------|
| **P0** | 補 `tests/buildEmailContent.test.js`（4 種 type 版型 + formatLINENotification 等）| 高（目前 0% 覆蓋）| 1h |
| **P0** | 補 `tests/notificationFormat.test.js` 加強（3 functions 但只測部分）| 中（既有 test 補強）| 30m |
| **P0** | 補 `tests/lineReply.test.js`（4 個 helper 全測）| 中（既有 test 補強）| 30m |
| **P1** | 補 `tests/csvReader.test.js` 加強（7 functions 覆蓋率不足）| 中（既有 test 補強）| 45m |
| **P1** | 補 `tests/knowledgeLoader.test.js`（loader.js 9 functions）| 中 | 1h |
| **P1** | 補 `tests/knowledgeTriggers.test.js`（triggers.js 8 functions）| 中 | 1h |
| **P2** | 重構 sheetsSync 雙 token 浪費（getFirstSheetName 收 accessToken 參數）| 低（功能改善）| 30m |
| **P2** | 補 `tests/sendImageMessage.test.js`（sendImageMessage 1 function）| 低 | 15m |
| **P3** | 文件化 `coverage-report.md`（自動跑 c8 / nyc 量化）| 長期 | 2h |

---

_本檔由 brtclaw 在 2026-07-29 12:38 Round 28 close-out 自動寫入_